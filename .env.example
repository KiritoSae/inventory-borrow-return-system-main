# Railway supplies these MYSQL* variables automatically when the MySQL service is linked.
MYSQLHOST=localhost
MYSQLPORT=3306
MYSQLDATABASE=inventory_system
MYSQLUSER=root
MYSQLPASSWORD=

# Use / on Railway; keep the local XAMPP folder path when serving from a subdirectory.
BASE_URL=/inventory-borrow-return-system-main
